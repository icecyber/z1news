/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  swcMinify: true,
  images: {
    domains: ['wp4.z1flexiblesolution.com'],
  },
};

module.exports = nextConfig;
